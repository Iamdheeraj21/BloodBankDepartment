<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bloodbankdepartment">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BloodBankDepartment"
        tools:ignore="AllowBackup">
        <activity android:name=".activites.Add_Notification_Activity"
            android:label="Create Notification"
            android:parentActivityName=".activites.MainActivity"/>
        <activity
            android:name=".activites.ForgetPassword"
            android:label="Forget Password"
            android:parentActivityName=".activites.LoginActivity" />
        <activity
            android:name=".activites.LoginActivity"
            android:label="Login Activity" />
        <activity android:name=".activites.MainActivity" />
        <activity android:name=".activites.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>